# R session artifacts
.Rhistory
.RData
.Ruserdata
Rplots.pdf

# targets pipeline cache (regenerate with targets::tar_make())
_targets/

# targets per-machine config (auto-written by tar_config_set / tar_config_get)
_targets.yaml

# generated figures (regenerate with pipeline)
fig/

# R project files
.Rproj.user
*.Rproj

# standalone validation scripts (not part of pipeline)
validate.R
validate_quick.R

# OS
.DS_Store

# Claude Code
.claude/
CLAUDE.md

# Operations log (working notes; never committed)
OPSLOG.md

# Internal planning notes — never committed
*PLAN*.md
*OPERATIONS-LOG*.md
*MANUSCRIPT-EDITS*.md
*MANUSCRIPT-ALIGN*.md
**/plans/
docs/superpowers/

# Python bytecode from the scripts/verify_amin_*.py certificates
__pycache__/
